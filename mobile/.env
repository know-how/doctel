EXPO_PUBLIC_API_BASE_URL=http://172.16.2.42:8000
# Interval (in ms) between WebSocket ping/pong (or HTTP polling fallback) checks.
# Controls how often the live connection monitor verifies backend connectivity.
# Default: 30000 (30 seconds)
EXPO_PUBLIC_WS_CHECK_INTERVAL_MS=30000
